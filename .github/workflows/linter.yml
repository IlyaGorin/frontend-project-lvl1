name: linter

on: push

jobs:
  testing:
    runs-on: ubuntu-latest
    steps:
      - name: check-project
      - uses: actions/checkout@v2
        uses: actions/setup-node@v1

        run: make lint
